{
  "name": "rs-launcher",
  "version": "0.2.0",
  "scripts": {
    "dev": "build-plugins.bat && tauri dev",
    "build": "build-plugins.bat release && tauri build",
    "build-plugins": "build-plugins.bat"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
